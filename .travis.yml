language: bash

services:
  - docker

before_install:
- docker pull nampdn/node-ipfs:latest
- container_id=$(docker run -d nampdn/node-ipfs /bin/sh -c "apk add bash jq curl outils-md5)

script:
- docker exec -it $container_id /bin/bash deploy.sh
